# Dokumen Aturan Irodori

Folder ini berisi aturan perubahan untuk level seri `library/irodori/`.

Gunakan dokumen utama berikut sebelum mengubah struktur atau isi seri:

- `rules.md`

Untuk aturan global lintas seri, lihat `library/dok/rules.md`.
